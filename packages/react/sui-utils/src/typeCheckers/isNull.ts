/**
 * Check if value is null
 *
 * @param {unknown} value value to be checked
 *
 * @return {boolean} Returns true if value is null
 */
export const isNull = (value: unknown): boolean => null === value
