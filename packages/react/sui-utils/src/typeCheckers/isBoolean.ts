/**
 * Check if value is boolean type
 *
 * @param {unknown} value Value to to be checked
 *
 * @return {boolean} Returns true if boolean
 */
export const isBoolean = (value: unknown): boolean => "boolean" === typeof value
