/**
 * Check if value is string
 *
 * @param {unknown} value Value to be checked
 * @return {boolean} Returns true if a variable is string
 */
export const isString = (value: unknown): boolean => "string" === typeof value
