/**
 * It is an internal method to render rest props list safely
 *
 * @param {Record<string, any>} propsList props list
 */
export const _renderHTMLPropsSafely = (propsList?: Record<string, any>) =>
	propsList
