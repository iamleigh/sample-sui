import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection, CommonPropsNotice } from '@wpmudev/sui-docs';

<PackageInstallGuide packageName="@wpmudev/sui-score" />

<Section title="Props" border={true}>
	<PropSection title="isSmall" type="boolean" defaultValue="false">
		If set to <Code small={true}> true </Code> the score will have a smaller size.
	</PropSection>
	<PropSection title="state" type="string">
		Depending on the task's requirements, particular task conditions can change the
		score type.

		Allowed options:
		<List>
			<span
				label={
					<>
						<Code small={true}>default</Code>: To display general progress on the score.
					</>
				}
			/>
			<span
				label={
					<>
						<Code small={true}>success</Code>: Generally display the low risk or safe state as progress. Score value is set from 0 - 34.
					</>
				}
			/>
			<span
				label={
					<>
						<Code small={true}>warning</Code>: Display a state or risk on progress with this state. Score value is set from 35 - 60.
					</>
				}
			/>
			<span
				label={
					<>
						<Code small={true}>error</Code>: Error state is to display the high risk and critical situation. Score value is set from 61 - 100.
					</>
				}
			/>
		</List>
	</PropSection>
	<PropSection title="bar" type="number" defaultValue="50">
		The length of the score bar to be displayed. it accepts integers from <Code small={true}>0</Code> to <Code small={true}>100</Code>.
	</PropSection>
	<PropSection title="value" type="number" defaultValue="50">
		Reflects the value of the score which will be shown as a label beside the score.
	</PropSection>
	<PropSection title="isPercentage" type="boolean" defaultValue="true">
		Places a percentage after the score text.
	</PropSection>
	<PropSection title="description" type="string">
		A text to be shown beside the value.
	</PropSection>
	<PropSection title="className" type="string">
		Add a class name to the score.
	</PropSection>
</Section>

<CommonPropsNotice />